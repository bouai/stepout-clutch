fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
httpx==0.28.1

# Postgres driver, used only when DATABASE_URL points at Postgres. Harmless on
# SQLite; required by any managed free tier with a persistent database.
psycopg[binary]==3.2.3
